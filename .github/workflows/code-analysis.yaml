name: practica-3-3-workflow

on: 
  push:
    branches: 
      - exercice-3


jobs: 
  exercice-3:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        name: "Code analysis"
      - run: echo "Realizando análisis de SAST"

      
      - name: "Check file"
        id: file_check
        uses: andstor/file-existence-action@v2
        with:
          files: "allow_pull_request.txt"
      
      - name: "File exists"
        uses: actions/checkout@v4
      - run: echo "${step.file_check.outputs.files_exists}"
      
  
  
