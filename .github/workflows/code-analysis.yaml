name: practica-3-3-workflow

on: 
  push:
    branches: 
      - exercice-3


jobs: 
  exercice-3:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        name: "Code analysis"
      - run: echo "Realizando análisis de SAST"

      - uses: actions/checkout@v4
        name: "Check requirements"
      - run: def x = test -f allow_pull_request.txt; echo "x is - ${x}"
        # def file = test -f allow_pull_request.txt
        # echo "${file}"
      
  
  
